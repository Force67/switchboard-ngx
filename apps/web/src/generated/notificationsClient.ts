// Generated by crudder. DO NOT EDIT.

import type { BulkUpdateResponse, ListNotificationsQuery, MarkNotificationReadRequest, Notification, NotificationResponse, NotificationsResponse, UnreadCountResponse } from "./notifications";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function getNotifications(apiBase: string, token: string, query?: Partial<ListNotificationsQuery>): Promise<Notification[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/notifications${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as NotificationsResponse;
    return json.notifications;
}

export async function getUnreadCount(apiBase: string, token: string): Promise<UnreadCountResponse> {
    const url = `${apiBase}/api/v1/notifications/count`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as UnreadCountResponse;
    return json as unknown as UnreadCountResponse;
}

export async function markNotificationRead(apiBase: string, token: string, notificationId: string, data: MarkNotificationReadRequest): Promise<Notification> {
    const url = `${apiBase}/api/v1/notifications/${notificationId}`;
    const response = await fetch(url, {
        method: "PUT",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as NotificationResponse;
    return json.notification;
}

export async function markAllRead(apiBase: string, token: string): Promise<BulkUpdateResponse> {
    const url = `${apiBase}/api/v1/notifications`;
    const response = await fetch(url, {
        method: "PUT",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as BulkUpdateResponse;
    return json as unknown as BulkUpdateResponse;
}

export async function deleteNotification(apiBase: string, token: string, notificationId: string): Promise<void> {
    const url = `${apiBase}/api/v1/notifications/${notificationId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
