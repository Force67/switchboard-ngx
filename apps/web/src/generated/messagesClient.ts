// Generated by crudder. DO NOT EDIT.

import type { CreateMessageRequest, ListMessagesQuery, MessageResponse, MessagesResponse, UpdateMessageRequest } from "./messages";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listMessages(apiBase: string, token: string, chatId: string, query?: Partial<ListMessagesQuery>): Promise<MessageResponse[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/chats/${chatId}/messages${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MessagesResponse;
    return json.messages;
}

export async function createMessage(apiBase: string, token: string, chatId: string, data: CreateMessageRequest): Promise<MessageResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/messages`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MessageResponse;
    return json as unknown as MessageResponse;
}

export async function getMessage(apiBase: string, token: string, chatId: string, messageId: string): Promise<MessageResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/messages/${messageId}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MessageResponse;
    return json as unknown as MessageResponse;
}

export async function updateMessage(apiBase: string, token: string, chatId: string, messageId: string, data: UpdateMessageRequest): Promise<MessageResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/messages/${messageId}`;
    const response = await fetch(url, {
        method: "PUT",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MessageResponse;
    return json as unknown as MessageResponse;
}

export async function deleteMessage(apiBase: string, token: string, chatId: string, messageId: string): Promise<void> {
    const url = `${apiBase}/api/v1/chats/${chatId}/messages/${messageId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
