// Generated by crudder. DO NOT EDIT.

import type { CreateInviteRequest, InviteResponse, InvitesResponse, ListInvitesQuery, RespondToInviteRequest } from "./invites";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listInvites(apiBase: string, token: string, chatId: string, query?: Partial<ListInvitesQuery>): Promise<InviteResponse[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/chats/${chatId}/invites${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as InvitesResponse;
    return json.invites;
}

export async function createInvite(apiBase: string, token: string, chatId: string, data: CreateInviteRequest): Promise<InviteResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/invites`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as InviteResponse;
    return json as unknown as InviteResponse;
}

export async function listUserInvites(apiBase: string, token: string, query?: Partial<ListInvitesQuery>): Promise<InviteResponse[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/invites${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as InvitesResponse;
    return json.invites;
}

export async function getInvite(apiBase: string, token: string, inviteId: string): Promise<InviteResponse> {
    const url = `${apiBase}/api/v1/invites/${inviteId}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as InviteResponse;
    return json as unknown as InviteResponse;
}

export async function respondToInvite(apiBase: string, token: string, inviteId: string, data: RespondToInviteRequest): Promise<InviteResponse> {
    const url = `${apiBase}/api/v1/invites/${inviteId}/respond`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as InviteResponse;
    return json as unknown as InviteResponse;
}

export async function deleteInvite(apiBase: string, token: string, inviteId: string): Promise<void> {
    const url = `${apiBase}/api/v1/invites/${inviteId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
