// Generated by crudder. DO NOT EDIT.

import type { GithubCallbackRequest, GithubLoginQuery, GithubLoginResponse, SessionResponse, UserResponse } from "./auth";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function githubLogin(apiBase: string, token: string, query?: Partial<GithubLoginQuery>): Promise<GithubLoginResponse> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/auth/github/login${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as GithubLoginResponse;
    return json as unknown as GithubLoginResponse;
}

export async function githubCallback(apiBase: string, token: string, data: GithubCallbackRequest): Promise<SessionResponse> {
    const url = `${apiBase}/api/v1/auth/github/callback`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as SessionResponse;
    return json as unknown as SessionResponse;
}

export async function logout(apiBase: string, token: string): Promise<void> {
    const url = `${apiBase}/api/v1/auth/logout`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}

export async function me(apiBase: string, token: string): Promise<UserResponse> {
    const url = `${apiBase}/api/v1/auth/me`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as UserResponse;
    return json as unknown as UserResponse;
}
