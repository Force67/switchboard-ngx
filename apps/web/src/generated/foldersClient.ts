// Generated by crudder. DO NOT EDIT.

import type { CreateFolderRequest, Folder, FolderResponse, FoldersResponse, UpdateFolderRequest } from "./folders";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listFolders(apiBase: string, token: string): Promise<Folder[]> {
    const url = `${apiBase}/api/v1/folders`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as FoldersResponse;
    return json.folders;
}

export async function createFolder(apiBase: string, token: string, data: CreateFolderRequest): Promise<Folder> {
    const url = `${apiBase}/api/v1/folders`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as FolderResponse;
    return json.folder;
}

export async function getFolder(apiBase: string, token: string, folderId: string): Promise<Folder> {
    const url = `${apiBase}/api/v1/folders/${folderId}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as FolderResponse;
    return json.folder;
}

export async function updateFolder(apiBase: string, token: string, folderId: string, data: UpdateFolderRequest): Promise<Folder> {
    const url = `${apiBase}/api/v1/folders/${folderId}`;
    const response = await fetch(url, {
        method: "PUT",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as FolderResponse;
    return json.folder;
}

export async function deleteFolder(apiBase: string, token: string, folderId: string): Promise<void> {
    const url = `${apiBase}/api/v1/folders/${folderId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
