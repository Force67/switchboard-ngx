# Frontend env template (copy to apps/web/.env and fill as needed)
VITE_API_BASE=http://localhost:7070
# Optional: if your backend serves auth callback somewhere else
# VITE_GITHUB_REDIRECT_PATH=/auth/callback
# Optional: force a default model id for new sessions
# VITE_DEFAULT_MODEL=openrouter/meta-llama/llama-3.1-70b-instruct
