// Generated by crudder. DO NOT EDIT.

use axum::{
    extract::{Extension, Query, Path, State},
    routing::{delete, get, post, put},
    Json, Router,
};
use std::sync::Arc;

use crate::error::GatewayResult;
use crate::rest::models::*;
use crate::state::GatewayState;
use crate::generated::rest::traits::members::MembersServiceTrait;

pub fn create_member_routes() -> Router<Arc<GatewayState>> {
    Router::new()
        .route("/chats/:chat_id/members", get(list_members))
        .route("/chats/:chat_id/members/:member_id", get(get_member).delete(remove_member))
        .route("/chats/:chat_id/members/:member_id/role", put(update_member_role))
        .route("/chats/:chat_id/leave", post(leave_chat))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/members",
    tag = "Members",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ListMembersQuery,
    ),
    responses((status = 200, description = "OK", body = MembersResponse)),
)]
pub async fn list_members(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Query(query): Query<ListMembersQuery>,
) -> GatewayResult<Json<MembersResponse>> {
    let out = state.members_service().list_members(user_id, chat_id, query).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/members/{member_id}",
    tag = "Members",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("member_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = MemberResponse)),
)]
pub async fn get_member(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, member_id)): Path<(String, String)>,
) -> GatewayResult<Json<MemberResponse>> {
    let out = state.members_service().get_member(user_id, chat_id, member_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    put,
    path = "/api/v1/chats/{chat_id}/members/{member_id}/role",
    tag = "Members",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("member_id" = String, Path, description = "Path parameter")
    ),
    request_body = UpdateMemberRoleRequest,
    responses((status = 200, description = "OK", body = MemberResponse)),
)]
pub async fn update_member_role(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, member_id)): Path<(String, String)>,
    Json(req): Json<UpdateMemberRoleRequest>,
) -> GatewayResult<Json<MemberResponse>> {
    let out = state.members_service().update_member_role(user_id, chat_id, member_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    delete,
    path = "/api/v1/chats/{chat_id}/members/{member_id}",
    tag = "Members",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("member_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn remove_member(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, member_id)): Path<(String, String)>,
) -> GatewayResult<()> {
    state.members_service().remove_member(user_id, chat_id, member_id).await?;
    Ok(())
}

#[utoipa::path(
    post,
    path = "/api/v1/chats/{chat_id}/leave",
    tag = "Members",
    params(
        ("chat_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn leave_chat(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
) -> GatewayResult<()> {
    state.members_service().leave_chat(user_id, chat_id).await?;
    Ok(())
}
