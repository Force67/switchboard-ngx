// Generated by crudder. DO NOT EDIT.

use axum::{
    extract::{Extension, Query, Path, State},
    routing::{delete, get, post},
    Json, Router,
};
use std::sync::Arc;

use crate::error::GatewayResult;
use crate::rest::models::*;
use crate::state::GatewayState;
use crate::generated::rest::traits::attachments::AttachmentsServiceTrait;

pub fn create_attachment_routes() -> Router<Arc<GatewayState>> {
    Router::new()
        .route("/chats/:chat_id/attachments", get(list_attachments))
        .route("/chats/:chat_id/messages/:message_id/attachments", get(list_message_attachments).post(create_attachment))
        .route("/chats/:chat_id/attachments/:attachment_id", get(get_attachment).delete(delete_attachment))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/attachments",
    tag = "Attachments",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ListAttachmentsQuery,
    ),
    responses((status = 200, description = "OK", body = AttachmentsResponse)),
)]
pub async fn list_attachments(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Query(query): Query<ListAttachmentsQuery>,
) -> GatewayResult<Json<AttachmentsResponse>> {
    let out = state.attachments_service().list_attachments(user_id, chat_id, query).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/messages/{message_id}/attachments",
    tag = "Attachments",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("message_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = AttachmentsResponse)),
)]
pub async fn list_message_attachments(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, message_id)): Path<(String, String)>,
) -> GatewayResult<Json<AttachmentsResponse>> {
    let out = state.attachments_service().list_message_attachments(user_id, chat_id, message_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    post,
    path = "/api/v1/chats/{chat_id}/messages/{message_id}/attachments",
    tag = "Attachments",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("message_id" = String, Path, description = "Path parameter")
    ),
    request_body = CreateAttachmentRequest,
    responses((status = 200, description = "OK", body = AttachmentResponse)),
)]
pub async fn create_attachment(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, message_id)): Path<(String, String)>,
    Json(req): Json<CreateAttachmentRequest>,
) -> GatewayResult<Json<AttachmentResponse>> {
    let out = state.attachments_service().create_attachment(user_id, chat_id, message_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/attachments/{attachment_id}",
    tag = "Attachments",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("attachment_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = AttachmentResponse)),
)]
pub async fn get_attachment(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, attachment_id)): Path<(String, String)>,
) -> GatewayResult<Json<AttachmentResponse>> {
    let out = state.attachments_service().get_attachment(user_id, chat_id, attachment_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    delete,
    path = "/api/v1/chats/{chat_id}/attachments/{attachment_id}",
    tag = "Attachments",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("attachment_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn delete_attachment(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, attachment_id)): Path<(String, String)>,
) -> GatewayResult<()> {
    state.attachments_service().delete_attachment(user_id, chat_id, attachment_id).await?;
    Ok(())
}
