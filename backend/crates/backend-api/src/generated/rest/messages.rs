// Generated by crudder. DO NOT EDIT.

use axum::{
    extract::{Extension, Query, Path, State},
    routing::{delete, get, post, put},
    Json, Router,
};
use std::sync::Arc;

use crate::error::GatewayResult;
use crate::rest::models::*;
use crate::state::GatewayState;
use crate::generated::rest::traits::messages::MessagesServiceTrait;

pub fn create_message_routes() -> Router<Arc<GatewayState>> {
    Router::new()
        .route("/chats/:chat_id/messages", get(list_messages).post(create_message))
        .route("/chats/:chat_id/messages/:message_id", get(get_message).put(update_message).delete(delete_message))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/messages",
    tag = "Messages",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ListMessagesQuery,
    ),
    responses((status = 200, description = "OK", body = MessagesResponse)),
)]
pub async fn list_messages(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Query(query): Query<ListMessagesQuery>,
) -> GatewayResult<Json<MessagesResponse>> {
    let out = state.messages_service().list_messages(user_id, chat_id, query).await?;
    Ok(Json(out))
}

#[utoipa::path(
    post,
    path = "/api/v1/chats/{chat_id}/messages",
    tag = "Messages",
    params(
        ("chat_id" = String, Path, description = "Path parameter")
    ),
    request_body = CreateMessageRequest,
    responses((status = 200, description = "OK", body = MessageResponse)),
)]
pub async fn create_message(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Json(req): Json<CreateMessageRequest>,
) -> GatewayResult<Json<MessageResponse>> {
    let out = state.messages_service().create_message(user_id, chat_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/messages/{message_id}",
    tag = "Messages",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("message_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = MessageResponse)),
)]
pub async fn get_message(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, message_id)): Path<(String, String)>,
) -> GatewayResult<Json<MessageResponse>> {
    let out = state.messages_service().get_message(user_id, chat_id, message_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    put,
    path = "/api/v1/chats/{chat_id}/messages/{message_id}",
    tag = "Messages",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("message_id" = String, Path, description = "Path parameter")
    ),
    request_body = UpdateMessageRequest,
    responses((status = 200, description = "OK", body = MessageResponse)),
)]
pub async fn update_message(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, message_id)): Path<(String, String)>,
    Json(req): Json<UpdateMessageRequest>,
) -> GatewayResult<Json<MessageResponse>> {
    let out = state.messages_service().update_message(user_id, chat_id, message_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    delete,
    path = "/api/v1/chats/{chat_id}/messages/{message_id}",
    tag = "Messages",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ("message_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn delete_message(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path((chat_id, message_id)): Path<(String, String)>,
) -> GatewayResult<()> {
    state.messages_service().delete_message(user_id, chat_id, message_id).await?;
    Ok(())
}
