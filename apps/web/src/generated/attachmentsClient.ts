// Generated by crudder. DO NOT EDIT.

import type { AttachmentResponse, AttachmentsResponse, CreateAttachmentRequest, ListAttachmentsQuery } from "./attachments";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listAttachments(apiBase: string, token: string, chatId: string, query?: Partial<ListAttachmentsQuery>): Promise<AttachmentResponse[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/chats/${chatId}/attachments${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as AttachmentsResponse;
    return json.attachments;
}

export async function listMessageAttachments(apiBase: string, token: string, chatId: string, messageId: string): Promise<AttachmentResponse[]> {
    const url = `${apiBase}/api/v1/chats/${chatId}/messages/${messageId}/attachments`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as AttachmentsResponse;
    return json.attachments;
}

export async function createAttachment(apiBase: string, token: string, chatId: string, messageId: string, data: CreateAttachmentRequest): Promise<AttachmentResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/messages/${messageId}/attachments`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as AttachmentResponse;
    return json as unknown as AttachmentResponse;
}

export async function getAttachment(apiBase: string, token: string, chatId: string, attachmentId: string): Promise<AttachmentResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/attachments/${attachmentId}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as AttachmentResponse;
    return json as unknown as AttachmentResponse;
}

export async function deleteAttachment(apiBase: string, token: string, chatId: string, attachmentId: string): Promise<void> {
    const url = `${apiBase}/api/v1/chats/${chatId}/attachments/${attachmentId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
