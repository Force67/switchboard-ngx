// Generated by crudder. DO NOT EDIT.

import type { ListMembersQuery, MemberResponse, MembersResponse, UpdateMemberRoleRequest } from "./members";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listMembers(apiBase: string, token: string, chatId: string, query?: Partial<ListMembersQuery>): Promise<MemberResponse[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/chats/${chatId}/members${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MembersResponse;
    return json.members;
}

export async function getMember(apiBase: string, token: string, chatId: string, memberId: string): Promise<MemberResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/members/${memberId}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MemberResponse;
    return json as unknown as MemberResponse;
}

export async function updateMemberRole(apiBase: string, token: string, chatId: string, memberId: string, data: UpdateMemberRoleRequest): Promise<MemberResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}/members/${memberId}/role`;
    const response = await fetch(url, {
        method: "PUT",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as MemberResponse;
    return json as unknown as MemberResponse;
}

export async function removeMember(apiBase: string, token: string, chatId: string, memberId: string): Promise<void> {
    const url = `${apiBase}/api/v1/chats/${chatId}/members/${memberId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}

export async function leaveChat(apiBase: string, token: string, chatId: string): Promise<void> {
    const url = `${apiBase}/api/v1/chats/${chatId}/leave`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
