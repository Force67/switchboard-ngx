// Generated by crudder. DO NOT EDIT.

import type { CreatePermissionRequest, Permission, PermissionResponse, PermissionsResponse } from "./permissions";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function getUserPermissions(apiBase: string, token: string, targetUserId: string): Promise<Permission[]> {
    const url = `${apiBase}/api/v1/users/${targetUserId}/permissions`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as PermissionsResponse;
    return json.permissions;
}

export async function getResourcePermissions(apiBase: string, token: string, resourceType: string, resourceId: string): Promise<Permission[]> {
    const url = `${apiBase}/api/v1/resources/${resourceType}/${resourceId}/permissions`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as PermissionsResponse;
    return json.permissions;
}

export async function createPermission(apiBase: string, token: string, resourceType: string, resourceId: string, data: CreatePermissionRequest): Promise<Permission> {
    const url = `${apiBase}/api/v1/resources/${resourceType}/${resourceId}/permissions`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as PermissionResponse;
    return json.permission;
}

export async function deletePermission(apiBase: string, token: string, permissionId: string): Promise<void> {
    const url = `${apiBase}/api/v1/permissions/${permissionId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
