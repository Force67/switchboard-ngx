// Generated by crudder. DO NOT EDIT.

import type { HealthResponse } from "./health";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function healthCheck(apiBase: string, token: string): Promise<HealthResponse> {
    const url = `${apiBase}/api/v1/health`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as HealthResponse;
    return json as unknown as HealthResponse;
}
