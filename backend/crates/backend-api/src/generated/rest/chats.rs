// Generated by crudder. DO NOT EDIT.

use axum::{
    extract::{Extension, Query, Path, State},
    routing::{delete, get, post, put},
    Json, Router,
};
use std::sync::Arc;

use crate::error::GatewayResult;
use crate::rest::models::*;
use crate::state::GatewayState;
use crate::generated::rest::traits::chats::ChatsServiceTrait;

pub fn create_chat_routes() -> Router<Arc<GatewayState>> {
    Router::new()
        .route("/chats", get(list_chats).post(create_chat))
        .route("/chats/:chat_id", get(get_chat).put(update_chat).delete(delete_chat))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats",
    tag = "Chats",
    params(
        ListChatsQuery,
    ),
    responses((status = 200, description = "OK", body = ChatsResponse)),
)]
pub async fn list_chats(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Query(query): Query<ListChatsQuery>,
) -> GatewayResult<Json<ChatsResponse>> {
    let out = state.chats_service().list_chats(user_id, query).await?;
    Ok(Json(out))
}

#[utoipa::path(
    post,
    path = "/api/v1/chats",
    tag = "Chats",
    request_body = CreateChatRequest,
    responses((status = 200, description = "OK", body = ChatResponse)),
)]
pub async fn create_chat(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Json(req): Json<CreateChatRequest>,
) -> GatewayResult<Json<ChatResponse>> {
    let out = state.chats_service().create_chat(user_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}",
    tag = "Chats",
    params(
        ("chat_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = ChatResponse)),
)]
pub async fn get_chat(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
) -> GatewayResult<Json<ChatResponse>> {
    let out = state.chats_service().get_chat(user_id, chat_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    put,
    path = "/api/v1/chats/{chat_id}",
    tag = "Chats",
    params(
        ("chat_id" = String, Path, description = "Path parameter")
    ),
    request_body = UpdateChatRequest,
    responses((status = 200, description = "OK", body = ChatResponse)),
)]
pub async fn update_chat(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Json(req): Json<UpdateChatRequest>,
) -> GatewayResult<Json<ChatResponse>> {
    let out = state.chats_service().update_chat(user_id, chat_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    delete,
    path = "/api/v1/chats/{chat_id}",
    tag = "Chats",
    params(
        ("chat_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn delete_chat(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
) -> GatewayResult<()> {
    state.chats_service().delete_chat(user_id, chat_id).await?;
    Ok(())
}
