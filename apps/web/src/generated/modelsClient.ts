// Generated by crudder. DO NOT EDIT.

import type { ModelSummary, ModelsResponse } from "./models";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listModels(apiBase: string, token: string): Promise<ModelSummary[]> {
    const url = `${apiBase}/api/v1/models`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as ModelsResponse;
    return json.models;
}
