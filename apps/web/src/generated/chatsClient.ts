// Generated by crudder. DO NOT EDIT.

import type { ChatResponse, ChatsResponse, CreateChatRequest, ListChatsQuery, UpdateChatRequest } from "./chats";

function authHeaders(token: string): HeadersInit {
    return {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
    };
}

export async function listChats(apiBase: string, token: string, query?: Partial<ListChatsQuery>): Promise<ChatResponse[]> {
    const params = new URLSearchParams();
    if (query) {
        for (const [key, value] of Object.entries(query)) {
            if (value !== undefined && value !== null) {
                params.set(key, String(value));
            }
        }
    }
    const queryString = params.toString();
    const url = `${apiBase}/api/v1/chats${queryString ? '?' + queryString : ''}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as ChatsResponse;
    return json.chats;
}

export async function createChat(apiBase: string, token: string, data: CreateChatRequest): Promise<ChatResponse> {
    const url = `${apiBase}/api/v1/chats`;
    const response = await fetch(url, {
        method: "POST",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as ChatResponse;
    return json as unknown as ChatResponse;
}

export async function getChat(apiBase: string, token: string, chatId: string): Promise<ChatResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}`;
    const response = await fetch(url, {
        method: "GET",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as ChatResponse;
    return json as unknown as ChatResponse;
}

export async function updateChat(apiBase: string, token: string, chatId: string, data: UpdateChatRequest): Promise<ChatResponse> {
    const url = `${apiBase}/api/v1/chats/${chatId}`;
    const response = await fetch(url, {
        method: "PUT",
        headers: authHeaders(token),
        body: JSON.stringify(data),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }

    const json = (await response.json()) as ChatResponse;
    return json as unknown as ChatResponse;
}

export async function deleteChat(apiBase: string, token: string, chatId: string): Promise<void> {
    const url = `${apiBase}/api/v1/chats/${chatId}`;
    const response = await fetch(url, {
        method: "DELETE",
        headers: authHeaders(token),
    });

    if (!response.ok) {
        throw new Error(`Request failed: ${response.status} ${response.statusText}`);
    }
}
