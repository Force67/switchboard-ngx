// Generated by crudder. DO NOT EDIT.

use axum::{
    extract::{Extension, Query, Path, State},
    routing::{delete, get, post},
    Json, Router,
};
use std::sync::Arc;

use crate::error::GatewayResult;
use crate::rest::models::*;
use crate::state::GatewayState;
use crate::generated::rest::traits::invites::InvitesServiceTrait;

pub fn create_invite_routes() -> Router<Arc<GatewayState>> {
    Router::new()
        .route("/chats/:chat_id/invites", get(list_invites).post(create_invite))
        .route("/invites", get(list_user_invites))
        .route("/invites/:invite_id", get(get_invite).delete(delete_invite))
        .route("/invites/:invite_id/respond", post(respond_to_invite))
}

#[utoipa::path(
    get,
    path = "/api/v1/chats/{chat_id}/invites",
    tag = "Invites",
    params(
        ("chat_id" = String, Path, description = "Path parameter"),
        ListInvitesQuery,
    ),
    responses((status = 200, description = "OK", body = InvitesResponse)),
)]
pub async fn list_invites(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Query(query): Query<ListInvitesQuery>,
) -> GatewayResult<Json<InvitesResponse>> {
    let out = state.invites_service().list_invites(user_id, chat_id, query).await?;
    Ok(Json(out))
}

#[utoipa::path(
    post,
    path = "/api/v1/chats/{chat_id}/invites",
    tag = "Invites",
    params(
        ("chat_id" = String, Path, description = "Path parameter")
    ),
    request_body = CreateInviteRequest,
    responses((status = 200, description = "OK", body = InviteResponse)),
)]
pub async fn create_invite(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(chat_id): Path<String>,
    Json(req): Json<CreateInviteRequest>,
) -> GatewayResult<Json<InviteResponse>> {
    let out = state.invites_service().create_invite(user_id, chat_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/invites",
    tag = "Invites",
    params(
        ListInvitesQuery,
    ),
    responses((status = 200, description = "OK", body = InvitesResponse)),
)]
pub async fn list_user_invites(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Query(query): Query<ListInvitesQuery>,
) -> GatewayResult<Json<InvitesResponse>> {
    let out = state.invites_service().list_user_invites(user_id, query).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/invites/{invite_id}",
    tag = "Invites",
    params(
        ("invite_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = InviteResponse)),
)]
pub async fn get_invite(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(invite_id): Path<String>,
) -> GatewayResult<Json<InviteResponse>> {
    let out = state.invites_service().get_invite(user_id, invite_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    post,
    path = "/api/v1/invites/{invite_id}/respond",
    tag = "Invites",
    params(
        ("invite_id" = String, Path, description = "Path parameter")
    ),
    request_body = RespondToInviteRequest,
    responses((status = 200, description = "OK", body = InviteResponse)),
)]
pub async fn respond_to_invite(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(invite_id): Path<String>,
    Json(req): Json<RespondToInviteRequest>,
) -> GatewayResult<Json<InviteResponse>> {
    let out = state.invites_service().respond_to_invite(user_id, invite_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    delete,
    path = "/api/v1/invites/{invite_id}",
    tag = "Invites",
    params(
        ("invite_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn delete_invite(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(invite_id): Path<String>,
) -> GatewayResult<()> {
    state.invites_service().delete_invite(user_id, invite_id).await?;
    Ok(())
}
