// Generated by crudder. DO NOT EDIT.

use axum::{
    extract::{Extension, Path, State},
    routing::{delete, get, post, put},
    Json, Router,
};
use std::sync::Arc;

use crate::error::GatewayResult;
use crate::rest::models::*;
use crate::state::GatewayState;
use crate::generated::rest::traits::folders::FoldersServiceTrait;

pub fn create_folder_routes() -> Router<Arc<GatewayState>> {
    Router::new()
        .route("/folders", get(list_folders).post(create_folder))
        .route("/folders/:folder_id", get(get_folder).put(update_folder).delete(delete_folder))
}

#[utoipa::path(
    get,
    path = "/api/v1/folders",
    tag = "Folders",
    responses((status = 200, description = "OK", body = FoldersResponse)),
)]
pub async fn list_folders(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
) -> GatewayResult<Json<FoldersResponse>> {
    let out = state.folders_service().list_folders(user_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    post,
    path = "/api/v1/folders",
    tag = "Folders",
    request_body = CreateFolderRequest,
    responses((status = 200, description = "OK", body = FolderResponse)),
)]
pub async fn create_folder(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Json(req): Json<CreateFolderRequest>,
) -> GatewayResult<Json<FolderResponse>> {
    let out = state.folders_service().create_folder(user_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    get,
    path = "/api/v1/folders/{folder_id}",
    tag = "Folders",
    params(
        ("folder_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK", body = FolderResponse)),
)]
pub async fn get_folder(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(folder_id): Path<String>,
) -> GatewayResult<Json<FolderResponse>> {
    let out = state.folders_service().get_folder(user_id, folder_id).await?;
    Ok(Json(out))
}

#[utoipa::path(
    put,
    path = "/api/v1/folders/{folder_id}",
    tag = "Folders",
    params(
        ("folder_id" = String, Path, description = "Path parameter")
    ),
    request_body = UpdateFolderRequest,
    responses((status = 200, description = "OK", body = FolderResponse)),
)]
pub async fn update_folder(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(folder_id): Path<String>,
    Json(req): Json<UpdateFolderRequest>,
) -> GatewayResult<Json<FolderResponse>> {
    let out = state.folders_service().update_folder(user_id, folder_id, req).await?;
    Ok(Json(out))
}

#[utoipa::path(
    delete,
    path = "/api/v1/folders/{folder_id}",
    tag = "Folders",
    params(
        ("folder_id" = String, Path, description = "Path parameter")
    ),
    responses((status = 200, description = "OK")),
)]
pub async fn delete_folder(
    State(state): State<Arc<GatewayState>>,
    Extension(user_id): Extension<i64>,
    Path(folder_id): Path<String>,
) -> GatewayResult<()> {
    state.folders_service().delete_folder(user_id, folder_id).await?;
    Ok(())
}
